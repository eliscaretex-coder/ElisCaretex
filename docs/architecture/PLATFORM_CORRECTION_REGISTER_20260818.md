# ElisCaretex V2 Correction Register

This register is the authoritative backlog for the platform review dated 2026-08-18. An item is closed only after its implementation and production validation are both recorded.

| ID | Priority | Issue | Status | Required next action |
| --- | --- | --- | --- | --- |
| P-001 | High | Module-specific visual rules created inconsistent spacing, controls, and hierarchy. | In progress | The shared enterprise UI foundation is loaded by every page. The Sorting, Finish, Distribution, and Trolleys workspace submenus now use one shared treatment; continue consolidating lower-level controls in small changes. |
| P-002 | Critical | The platform's `SECURITY DEFINER` functions require a live authorization and grant audit before wider access. | Pending live audit | Review caller authorization, `search_path`, `PUBLIC` execute privileges, and RLS behavior in the connected Supabase project. |
| P-003 | Critical | Roles are named consistently, but there is no central permission catalogue for individual business actions. | Pending design | Create stable permission keys and enforce them in every protected RPC; use the same catalogue for browser visibility. |
| P-004 | High | Documentation was split across old snapshots and the repository README did not match the current project baseline. | Implemented - validate | The root README, documentation guide and canonical architecture overview now point only to active references. Superseded snapshot/handover evidence was removed from the active source tree. |
| P-005 | High | Distribution actuals/history migration has no matching validation SQL in this repository snapshot. | Pending | Add and run validation for `202608170001_distribution_actuals_history.sql`. |
| P-006 | High | Legacy frontend fallbacks can hide incomplete migrations. | Pending | Inventory each fallback, add an owner and retirement date, then remove it after production verification. |
| P-007 | High | Sorting, Customers, and Roster browser modules are excessively large. | Pending | Split by domain responsibility while preserving public page behavior and test each boundary. |
| P-008 | Critical | Development simulation records exist alongside real master data. | Partially blocked by integrity gate | The simulation-only Finish snapshots have no remaining records. The full-day simulation has 137 production-flow events and linked Washing, MOP, and Trolley records; its cleanup gate found 6 later trolley events and 2 later trolley stays, so it was intentionally not deleted to avoid removing later operational activity. Reconcile those trolley links before retrying the tagged cleanup. |
| P-009 | Medium | Distribution schedule was exposed as a Customers submenu even though it belongs to Distribution operations. | Implemented - validate | Customers no longer exposes the main Distribution tab; legacy URLs redirect to Distribution. Route customers is now a weekly delivery planner driven by the published Schedule Planner: each day-and-route group retains its defined delivery order. |
| P-010 | Medium | The global date picker appeared in the Customers header without a clear purpose. | Implemented - validate | It is now shown only in the Schedule Planner toolbar, where it selects the schedule effective date. |
| P-011 | High | Finish used dense snapshot styling that made the workspace feel compressed. | Implemented - validate | Restore readable spacing and type, preserve the existing Finish subtabs, and stack the queue below the ledger on narrower screens. |
| P-012 | High | Development-data warnings appear in operational workspaces. | Implemented - partial cleanup pending | The browser warnings were removed from Finish, Finish Results, Sorting, MOP, Production Tracker, and Trolleys. The only remaining tagged records are protected full-day simulation records pending trolley-link reconciliation. |
| P-013 | Medium | Finish Results was constrained to a narrow work area and used overly compact result rows. | Implemented - validate | It now uses the full operational work surface with larger result summaries and production-detail rows, while retaining responsive behavior on smaller screens. |
| P-014 | Critical | Production terminals had no fixed device identity; an authenticated user could operate from any browser and select another operator in key flows. | Implemented - rollout validation required | Migration `202608180001_production_terminal_access.sql` binds each Finish/Sorting computer to a low-privilege terminal account and real station. No staff member logs in or enters a PIN on the production computer: staff attribution remains the existing published Roster/Actual selection. Primary Finish/Washing/Reception writes are server-side checked against the fixed terminal. Complete the follow-up enforcement for advanced Sorting MOP, staff-adjustment and exception RPCs before permitting those actions on shared terminals. |
| P-015 | High | Administrators could not see one consolidated list of application accounts, assigned roles, terminal identities, and navigation access. | Implemented - validate | `202608180002_admin_account_access_directory.sql` provides an administrator-only account inventory. Staff Master now includes an Accounts & Access view that shows the Supabase account, roles, derived modules and registered terminal binding. |
| P-016 | Critical | Administrators could not recover access to a production computer without changing its protected terminal configuration. | Implemented - validate | Accounts & Access now provides an administrator-only password reset for registered production computers. The prior password remains intentionally unrecoverable; an administrator sets and confirms a new password without changing the computer, station, roster attribution, or permissions. Validate the recovery procedure with a spare terminal before wider rollout. |
| P-017 | High | Finish trolley validation treated Medium and Large as completely distinct even where a Large trolley is operationally acceptable for a Medium requirement. | Implemented - validate | `202608260001_finish_medium_large_trolley_compatibility.sql` allows an unreserved Large trolley to satisfy a Medium Finish requirement. Small, Large, Gray, and all other requirements remain exact; MOP already validates trolley quantity only and did not reject this substitution. |
| P-018 | High | Finish production with a scanned trolley could be blocked when the Production Flow was opened before its planned production date; the backend error was rendered behind the modal and was not readable. | Implemented - validate | Trolley assignment now accepts dates from the Production Flow opening date through its Delivery Date, and Finish displays save errors inside the active registration window. |
| P-019 | High | The initial early-production correction still restricted Finish customers to the Production Flow opening date, excluding valid future customers and customers due for delivery today. | Implemented - validate | `202608270001_finish_delivery_date_eligibility.sql` permits Finish activity when the Delivery Date is today or later, while refusing a customer whose Delivery Date is already in the past. |
| P-020 | High | Finish staff attribution was restricted to the published Roster or a pre-existing Actual entry, which did not support a staff member confirming their own attendance when the roster changed. | Implemented - validate | Finish now offers a staff-name confirmation flow: the Roster remains the planned reference, while any active production staff member can be confirmed to the actual Finish table without changing the published roster. |
| P-021 | Medium | The Finish Staff workspace presented operational attendance as dense performance cards, making the simple check-in and correction workflow difficult to scan. | Implemented - validate | The workspace now uses a compact operational table with planned position, actual table, hours, status, and one confirmation/edit action per staff member. |
| P-022 | High | A Finish terminal could display and reassign staff for all Finish tables even though the account is registered to one specific table. | Implemented - validate | A table-bound terminal now displays and confirms only staff for its assigned table; central, non-table-bound access retains the full Finish view. |
| P-023 | High | Finish Actual staffing could not record a reversible absence, an early departure, or a timed move between Tables; changing a person to another Finish Table assigned all worked time to the latest table. | Implemented - validate | `202608270003_finish_staff_attendance_table_transfers.sql` and `202608270005_finish_staff_early_departure.sql` add a reversible Actual attendance overlay, explicit timed table segments, and early-departure closure. A table-bound terminal can mark its rostered person absent, later confirm the arrival, transfer a confirmed person, or close their current shift at an exact time. Finish metrics allocate each segment, including proportional break/non-work time, to the correct table. |
| P-024 | High | Staff time editing did not reliably pre-fill the effective roster times, overtime was not explicit, and Finish staff with a valid Sorting cover capability could not be transferred into Sorting without losing the time split between areas. Sorting Staff also used a different, card-heavy layout. | Implemented - validate | `202608270006_finish_sorting_cover_transfer_and_overtime.sql` pre-fills Finish Actual time from the effective Roster, derives overtime from a later actual leaving time, and adds the auditable transfer. `202608270007_finish_sorting_cover_capability_gate.sql` restricts the transfer to Staff Master records with an active `SORTING_AREA` COVER capability on that business date. The move closes Finish, opens a Sorting Actual session and allocates the original break once across the two areas. Sorting Staff now uses the same compact operational-table pattern as Finish. |
| P-025 | High | Worked time could be read without a visible breakdown of the actual break, and the rare operational exception of a different break could not be recorded consistently in Finish and Sorting. | Implemented - validate | `202608270008_actual_staff_break_editing.sql` adds audited Actual break editing in Finish and Sorting. Worked time now deducts the saved break; Finish shows the same `Worked` column as Sorting and both staff editors expose the break in minutes. |
| P-026 | Critical | Staff-time edits could be saved without a clear before-save result, and an incorrect span could make later correction difficult. The edit screen could also turn an open Finish shift into a recorded departure without an explicit confirmation. | Implemented - validate | `202608270009_staff_time_preview_guard_and_finish_correction.sql` enforces a maximum of 13 recorded hours per staff member per day at database level. Finish and Sorting now show a time, break and net-work preview requiring confirmation before saving. Finish keeps a shift open until an actual departure is explicitly recorded, shows planned net hours for unconfirmed staff, and allows table-transfer records to be corrected while preserving their Table moves. |
| P-027 | High | Finish staff editing needed to retain the roster start/end reference without turning an open shift into a recorded departure. Evening work through 03:00 also needed to remain on the workday in which the shift started. | Implemented - validate | Finish now pre-fills the effective start and planned leaving time as an editable reference, but requires an explicit departure confirmation before closing the shift. Invalid placeholder start values such as `0` are ignored. `202608270010_evening_shift_boundary_inclusive.sql` assigns Evening work at or before the configured 03:00 boundary to the originating business date. |
| P-028 | Medium | The manual-staff command in Finish implied immediate confirmation, and Sorting Staff diverged visually from the Finish operational table despite serving the same attendance workflow. | Implemented - validate | Finish now names the action `Add staff manually`, including the fixed Table when applicable. Sorting Staff now uses the Finish header, staff-count, table density, status treatment, and the same Actual Staffing editor: staff member, Actual Position, start, leaving time, break, explicit close-shift confirmation, note, and save. The existing audited time-away value is preserved when this focused editor is used. An unchecked close-shift checkbox retains the leaving time only as a reference, including when someone starts later than the planned end, and keeps the shift open. |
| P-029 | Medium | Sorting Staff action controls used a mix of filled teal, purple, green, and red styles that did not match the Finish operational table, and its Actual position column showed only the generic Sorting area. | Implemented - validate | Sorting now uses the Finish-style compact outlined action controls, with destructive actions retained as red outlines. Actual position shows the active `MOP` or `Clothes` assignment, while moves to another area remain visible as the destination area. |
| P-030 | High | Sorting `No Work` exposed an overly broad Move path to MOP, Washing, and Distribution, without assigning a Finish Table. | Implemented - validate | Sorting `Move` now offers only Finish Table 1, 2, or 3. The database validates the named Finish station, blocks a whole-shift move after Sorting production activity, records Finish Actual attendance and table attribution, preserves the Roster, and audits both the transfer and its reversal. Existing historical moves retain a controlled return to Sorting without restoring the deprecated broad Move command. |
| P-031 | High | The registered Sorting computer had the `SORTING_OPERATOR` role but could not open MOP Production because the MOP scope required a linked personal Staff record. | Implemented - validate | `202608270015_sorting_terminal_mop_access.sql` permits an active registered Sorting terminal to use MOP Production without a personal Staff identity, while retaining role checks. MOP Types Management remains visible as read-only; only ADMIN or MANAGER can create or modify master data. |
| P-032 | Medium | The Finish customer queue did not distinguish a planned customer from one already received in Trolley Intake before washing, and repeated waiting text instead of showing the planned date. | Implemented - validate | `202608270016_finish_queue_trolley_intake_status.sql` adds Clothes trolley-intake evidence to the shared Tracker consumed by Finish. The Finish queue now uses the scheduled weekday/date in its compact badge and labels the operational stage as awaiting intake, received at intake, washed and waiting for Finish, or ready for Finish. |
| P-033 | High | Finish had no persistent operational guard for a washed customer due for delivery today that still lacked Finish evidence after the midday cutoff. The list could also not extend safely into physically received future work. | Implemented - validate | `202608270017_finish_operational_queue_and_delivery_reconciliation.sql` adds a controlled Finish operational queue and a delivery-day reconciliation record; `202608270018_finish_operational_queue_display_fields.sql` keeps the customer, route, order and colour visible for future physical work. The queue admits the full current scheduled day, plus the complete latest delayed day only while it still has unresolved physical work, and the complete earliest advanced day once physical work has begun. Past confirmed/really processed rows remain visible only within that active continuation day and are marked as closed; they never enter production metrics. `202608270020_finish_delivery_reconciliation_quantity_unit.sql` records only a positive approximate quantity in the chosen unit (KG or Units), never a value matched to washed KG, and keeps it outside production metrics. |
| P-034 | High | Finish had no governed ReWash record per Table and Shift. ReWash work could not be distinguished from normal customer flow, and approximate delivery reconciliation risked being mistaken for actual productivity. | Implemented - validate | `202608270019_finish_rewash_operational_ledger.sql` creates one audited, versioned ReWash record for each Finish Table / Shift / operational date. Each customer line requires an active Clothes or Others customer and real KG; batch remains optional and trolley is explicitly not required. ReWash is separately visible in Results but included in real production only after saving. Delivery reconciliation records remain outside all production, Table, Shift and staff metrics. |
| P-035 | High | A customer acknowledged as `NOT_PROCESSED` at the delivery check could still be blocked by the normal delivery cutoff when staff attempted to process it before its two-hour reminder elapsed. | Implemented - validate | `202608280023_finish_late_acknowledged_processing.sql` preserves the normal 12:00 delivery cutoff, but permits a real Finish record only for a customer with an open `NOT_PROCESSED` acknowledgement. The normal batch, processor, table, trolley and actual-business-date rules still apply, so only a real saved production record enters metrics. |
| P-036 | Medium | Correcting a Staff Master primary role after saving a roster left the roster displaying the old role, while silently changing a published roster would have compromised history. | Implemented - validate | `202609090024_staff_primary_role_current_roster_choice.sql`, `202609090025_staff_primary_role_stale_draft_repair.sql` and `202609090026_staff_primary_role_future_roster_drafts.sql` add an explicit Staff Master choice: apply the correction only to matching `BASE` assignments in editable current and future drafts, or keep each draft as planned. COVER assignments, locations, past rosters and all published roster versions remain unchanged. The selected draft update is atomic with the Staff Master change and is audited. |
| P-037 | Critical | Finish reconciliation checked only customers whose Delivery Date was today, allowing a washed customer from an earlier delivery date to remain in the operational queue without a required decision. Protected simulation washes could also be mistaken for real operational evidence. | Implemented - validate | `202609090027_finish_overdue_reconciliation_backlog.sql` makes every washed Clothes customer past the Delivery Date midday cutoff enter the blocking reconciliation backlog until a real Finish record, processed confirmation, or a two-hour `NOT_PROCESSED` acknowledgement applies. `202609090028_finish_reconciliation_exclude_simulated_washes.sql` excludes `SIM_` wash sources from this operational control while their protected cleanup remains pending. The acknowledgement remains outside all Finish metrics. |
| P-038 | High | A manually added Finish staff member could open the Actual form without a clear start-time value, while the Table-time display read timestamp text before converting it to Ireland local time. This could make a valid actual record look unrelated to the recorded shift. | Implemented - validate | Manual Finish check-in now proposes the current Ireland local time, requires an explicit actual start time in both the screen and database, and retains the existing confirmation step. Timestamp values in the staff grid are converted to Ireland local time before rendering, so Table time matches the saved actual session. |
| P-039 | High | Finish recorded KG and Units as distinct traceable quantities but did not convert Units to KG for the operational total, Table productivity or Results efficiency. | Implemented - validate | Roster Settings now governs `Finish units per kg`, initially 6. Finish retains the exact entered KG and Units, shows the converted KG equivalent in the production form, and uses the same configured conversion for Finish Table metrics and Results. |
| P-040 | Medium | The Production Tracker exposed complete route-level evidence in a wide analytical board, but supervisors lacked a fast list view for scanning each scheduled stream and spotting production gaps. | Implemented - validate | Tracker now has an `Operational list` view beside the existing Route board. It keeps the same date, search and status filters, groups streams by route colour and priority, and shows current status, processed or washed KG/Units, batch references and physical trolley numbers in one compact row. |
| P-041 | Medium | The shared Tracker did not expose the batch reference saved on Finish production lines, so completed Clothes rows in the operational view showed no batch. | Implemented - validate | The Tracker contract now returns the distinct active Finish batch references for each Clothes flow item. MOP rows continue to show their existing external batch evidence when present. |

## Development data cleanup order

Run these scripts in the Supabase SQL editor as an owner with full database access, one at a time, in this order:

1. `docs/testing/CLEANUP-SIM_FULL_DAY_20260806_V1.sql`
2. `docs/testing/CLEANUP-SIM_FINISH_SNAPSHOT_114_V1.sql`
3. `docs/testing/CLEANUP-SIM_FINISH-SNAPSHOT-115-V1.sql`

Each script targets only a named simulation marker. The full-day cleanup contains a safety gate that stops when later non-simulation trolley activity is found. Preserve the displayed result from every execution in the release evidence.

## Acceptance criteria for wider rollout

- The live Supabase security advisors are reviewed and all critical findings are resolved or formally accepted.
- The permission catalogue has database-backed tests for every role/action combination.
- Tagged development simulations are absent, with cleanup evidence retained.
- Distribution actuals/history validation passes in the target environment.
- Operational users approve the shared UI in Customers, Distribution, Finish, Sorting, and Production Tracker.
